<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Open .md/.markdown tapped in a file manager.
                 Multiple <data pathPattern> are OR'd; scheme+mimeType+pattern are AND'd, so this
                 matches only text/* md/markdown URIs — no false positives on other files. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="file" />
                <data android:mimeType="text/*" />
                <data android:pathPattern=".*\.md" />
                <data android:pathPattern=".*\.MD" />
                <data android:pathPattern=".*\.markdown" />
                <data android:pathPattern=".*\.MARKDOWN" />
            </intent-filter>

            <!-- content:// URIs from SAF (Samsung Files on Android 13+ hands these out).
                 No BROWSABLE here — file managers don't need a browser fallback for this. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:mimeType="text/*" />
                <data android:pathPattern=".*\.md" />
                <data android:pathPattern=".*\.MD" />
                <data android:pathPattern=".*\.markdown" />
                <data android:pathPattern=".*\.MARKDOWN" />
            </intent-filter>

            <!-- "Share to MD Reader": apps that export EXTRA_STREAM with a text/* md file. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- INTERNET: Capacitor localhost bridge + future web links. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Shared storage reads for the Browse navigator on API ≤ 32 (scoped-storage legacy). -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
</manifest>
